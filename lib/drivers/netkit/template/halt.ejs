if `<%= vlist %> | grep <%= name %> > /dev/null`
then
<%= vhalt %> <%= name %> > /dev/null
else
exit 0
fi
if `<%= vlist %> | grep <%= name %> > /dev/null`
then
<%= vcrash %> <%= name %> > /dev/null
else
exit 0
fi
if `<%= vlist %> | grep <%= name %> > /dev/null`
then
exit 2
else
exit 0
fi
